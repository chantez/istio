apiVersion: v1
kind: Service
metadata:
  name: service2-istio
  labels:
    app: service2-istio
spec:
  ports:
  - port: 8091
    name: http
  selector:
    app: service2-istio
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service2-istio-v1
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service2-istio
        version: v1
    spec:
      containers:
      - name: service2-istio
        image: chantez/servic1-istio:1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8091
---

###########################################################################
# (gateway)
##########################################################################
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: service2-istio-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: service2-istio
spec:
  hosts:
  - "*"
  gateways:
  - service2-istio-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: service2-istio
        port:
          number: 8091