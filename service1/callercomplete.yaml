apiVersion: v1
kind: Service
metadata:
  name: service3-istio
  labels:
    app: service3-istio
spec:
  ports:
  - port: 8091
    name: http
  selector:
    app: service3-istio
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: service3-istio-v1
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: service3-istio
        version: v1
    spec:
      containers:
      - name: service3-istio
        image: chantez/service1-istio:1.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8091
---

###########################################################################
# (gateway)
##########################################################################
apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: service3-istio-gateway
spec:
  selector:
    istio: ingressgateway # use istio default controller
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: service3-istio
spec:
  hosts:
  - "*"
  gateways:
  - service3-istio-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: service3-istio
        port:
          number: 8091